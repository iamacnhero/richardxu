程序运行，需要具备:
- Maven 3 (http://maven.apache.org/download.html#Installation).
- Apache Hadoop 环境.
- 在 application.properties 中配置输入输出目录，并上传一个nginx日志文件到输入目录.
- 运行 mvn clean assembly:assembly(or mvn -Dmaven.test.skip=true assembly:assembly, skip test)
- 目标程序生成在 target 目录
- Unzip mapreduce-bin.zip 到Hadoop服务器
- 运行脚本 (startup.sh)
